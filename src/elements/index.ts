// Import and register all custom elements
import "./ButtonElement.svelte";
import "./CardElement.svelte";
import "./InputElement.svelte";

// Export any utility functions for custom elements
export const version = "1.0.0";
